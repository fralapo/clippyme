# CPU-only / Apple Silicon / ARM override
# Removes GPU reservation and uses python:3.11-slim base
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.cpu.yml up --build

name: clippyme

services:
  backend:
    build:
      args:
        GPU_RUNTIME: cpu
    deploy: {}
